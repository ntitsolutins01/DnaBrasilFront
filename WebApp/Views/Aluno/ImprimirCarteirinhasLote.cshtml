@model WebApp.Models.AlunoModel
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Impressão de Carteirinhas em Lote</title>
    <link rel="stylesheet" href="~/assets/styles_Carteirinha/impressao-lote-carteirinha.css" />
    <link rel="stylesheet" href="~/assets/vendor/font-awesome/css/font-awesome.css" />
</head>
<body>
    <div class="print-controls">
        <button class="btn btn-primary" onclick="printFull()">
            <i class="fa fa-print"></i> Imprimir Carteirinha Completa
        </button>
        <button class="btn btn-primary" onclick="printVerso()">
            <i class="fa fa-print"></i> Imprimir Apenas Verso
        </button>
        <button class="btn btn-secondary" onclick="window.close()">
            <i class="fa fa-times"></i> Fechar
        </button>
        <div class="pagination-controls">
            <span id="total-info">Total de carteirinhas: @Model.Alunos.Count()</span>
        </div>
    </div>

    <div class="preview-wrapper">
        @foreach (var aluno in Model.Alunos)
        {
            <div class="preview-container">
                <!-- Frente da carteirinha -->
                <div class="page frente-page" style="background-image: url(/assets/styles_Carteirinha/modelos/carteirinha-frente.png);">
                    <div class="student-info">
                        <div class="info-row">
                            <label>NOME DO ESTUDANTE:</label>
                            <span>@aluno.Nome</span>
                        </div>
                        <div class="info-row">
                            <label>DATA DE NASCIMENTO:</label>
                            <span>@aluno.DtNascimento</span>
                        </div>
                        <div class="info-row">
                            <label>TELEFONE:</label>
                            <span>@aluno.Celular</span>
                        </div>
                        <div class="info-row">
                            <label>CPF:</label>
                            <span>@aluno.Cpf</span>
                        </div>
                        <div class="info-row">
                            <label>MATRÍCULA:</label>
                            <span>@aluno.Id</span>
                        </div>
                        <div class="activity-info">
                            @aluno.Modalidades
                        </div>
                    </div>
                    <div class="photo-container">
                        @if (aluno.ByteImage != null && aluno.ByteImage.Length > 0)
                        {
                            var base64 = Convert.ToBase64String(aluno.ByteImage);
                            var imgSrc = String.Format("data:image/gif;base64,{0}", base64);
                            <img src="@imgSrc" alt="Foto do estudante" class="student-photo">
                        }
                        else
                        {
                            <img src="@(aluno.Sexo == "Feminino" ? "/assets/images/menina.png" : "/assets/images/menino.png")"
                                 alt="Foto do estudante" class="student-photo" style="object-fit: inherit!important;">
                        }
                    </div>
                    <div class="qr-code-container">
                        @if (aluno.QrCode != null)
                        {
                            var base64QrCode = Convert.ToBase64String(aluno.QrCode);
                            var imgSrcQrCode = String.Format("data:image/gif;base64,{0}", base64QrCode);
                            <img src="@imgSrcQrCode" alt="QR Code" class="qr-code">
                        }
                    </div>
                </div>

                <!-- Verso da carteirinha -->
                <div class="page verso-page" style="background-image: url(/assets/styles_Carteirinha/modelos/@Model.ModeloCarteirinha?.NomeImagemVerso);">
                    <div class="main-content-verso">
                        <div class="info-row-verso">
                            <label>MUNICÍPIO/ESTADO:</label>
                            <span>@aluno.MunicipioEstado</span>
                        </div>
                        <div class="info-row-verso">
                            <label>UNIDADE ESCOLAR:</label>
                            <span>@aluno.NomeLocalidade</span>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <script>
        function setupPrintConfig() {
            if (window.matchMedia) {
                const mediaQueryList = window.matchMedia('print');
                mediaQueryList.addListener(function(mql) {
                    if (mql.matches) {
                        document.body.style.webkitPrintColorAdjust = 'exact';
                        document.body.style.printColorAdjust = 'exact';
                    }
                });
            }
        }

        function waitForImages(callback) {
            const images = document.getElementsByTagName('img');
            let loadedImages = 0;
            const totalImages = images.length;

            function imageLoaded() {
                loadedImages++;
                if (loadedImages === totalImages) {
                    callback();
                }
            }

            Array.from(images).forEach(img => {
                if (img.complete) {
                    imageLoaded();
                } else {
                    img.addEventListener('load', imageLoaded);
                    img.addEventListener('error', imageLoaded);
                }
            });

            // Se não houver imagens, execute o callback imediatamente
            if (totalImages === 0) {
                callback();
            }
        }

        function printFull() {
            setupPrintConfig();

            document.querySelectorAll('.frente-page').forEach(page => {
                page.classList.remove('no-print');
            });

            document.querySelectorAll('.verso-page').forEach(page => {
                page.classList.remove('no-print');
            });

            waitForImages(() => {
                setTimeout(() => {
                    window.print();
                }, 100);
            });
        }

        function printVerso() {
            setupPrintConfig();

            document.querySelectorAll('.frente-page').forEach(page => {
                page.classList.add('no-print');
            });

            document.querySelectorAll('.verso-page').forEach(page => {
                page.classList.remove('no-print');
            });

            waitForImages(() => {
                setTimeout(() => {
                    window.print();
                }, 100);
            });
        }

        // Configurar listener para evento de beforeprint
        if (window.matchMedia) {
            window.matchMedia('print').addListener(function(mql) {
                if (mql.matches) {
                    document.body.style.webkitPrintColorAdjust = 'exact';
                    document.body.style.printColorAdjust = 'exact';
                }
            });
        }

        // Garantir que as configurações sejam aplicadas quando a página carregar
        document.addEventListener('DOMContentLoaded', setupPrintConfig);
    </script>
</body>
</html>