192.252.197.180:443
http://191.252.197.180:8000/


@{
    ViewData["Title"] = "Catálogo de Cursos";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div id="vCurso">
    <div class="row">
        <div class="col-md-12">
            @using (@Html.BeginForm("Create", "Curso", FormMethod.Post, new { id = "formCurso", @class = "form-horizontal", @enctype = "multipart/form-data" }))

            {
                        <section class="panel">
                            <header class="panel-heading">
                                <div class="panel-actions">
                                    <a href="#" class="panel-action panel-action-toggle" data-panel-toggle></a>
                                </div>

                                <h2 class="panel-title">Catálogo de Cursos</h2>
                            </header>
                            <div class="panel-body">



                            </div>
                            <footer class="panel-footer">
                                <div class="row">
                                    <div class="col-md-9 col-md-offset-3">
                                        <a href="@Url.Action("Index", "Dashboard")" class="btn btn-dark pull-right" style="margin-left: 5px;margin-right: 5px;"><i class="fa fa-reply"></i> &nbsp;&nbsp;Voltar</a>
                                    </div>
                                </div>
                            </footer>
                        </section>
            }
        </div>
    </div>
</div>
@section CssSpecificVendor {
}
@section Scripts {

            <script>

                (function ($) {

                    'use strict';
                    var crud = @ViewBag.CrudMessage;

                    if (crud === 1) {
                        new PNotify({
                            title: 'Catálogo Curso',
                            text: 'Cadastro realizado com sucesso!',
                            type: 'success'
                        });
                    } else if (crud === 2) {
                        new PNotify({
                            title: 'Catálogo Curso',
                            text: 'Dados alterados com sucesso.',
                            type: 'warning'
                        });
                    } else if (crud === 3) {
                        new PNotify({
                            title: 'Catálogo Curso',
                            text: 'Registro excluído com sucesso.',
                            type: 'error'
                        });
                    }

                    var notify = @ViewBag.NotifyMessage;
                    var messagem = '@ViewBag.Notify';

                    if (notify === 0) {
                        new PNotify({
                            title: 'Catálogo Curso',
                            text: messagem,
                            type: 'success'
                        });
                    } else if (notify === 1) {
                        new PNotify({
                            title: 'Catálogo Curso',
                            text: messagem,
                            type: 'warning'
                        });
                    } else if (notify === 2) {
                        new PNotify({
                            title: 'Catálogo Curso',
                            text: messagem,
                            type: 'error'
                        });
                    }

                }).apply(this, [jQuery]);

            </script>
}